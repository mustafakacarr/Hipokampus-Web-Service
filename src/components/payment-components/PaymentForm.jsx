import React from 'react';
import OrderLayout from '../../layouts/OrderLayout';

const PaymentForm = () => {
    return (
        <OrderLayout> here is
            the payment form
      </OrderLayout>
    );
};

export default PaymentForm;